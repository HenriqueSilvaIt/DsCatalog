import HomeCard from "../../../components/HomeCard";

import './style.css';

export default function HomeBody() {
    return (
            <main>
                <section>

                    <div className="dscatalog-container">
                        <HomeCard />
                    </div>

                </section>

            </main>

    );
}